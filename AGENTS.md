允许批量删除文件或目录，但必须满足以下条件：

- 用户已经明确授权删除目标及范围。
- 删除前必须列出并核验每个目标的绝对路径。
- 所有目标必须位于当前项目目录内。
- 禁止把项目根目录、磁盘根目录、用户目录、未解析变量、通配符或模糊计算路径作为递归删除目标。
- 优先删除功能边界明确、可由版本控制恢复的目录或文件，并在删除后检查版本控制状态。

对于经过上述核验的明确项目内目录，允许使用 `Remove-Item -LiteralPath <明确绝对路径> -Recurse`。仍禁止使用 `del /s`、`rd /s`、`rmdir /s`、`rm -rf`，以及任何未经核验的递归删除命令。

## Unity 陈旧锁文件

`Temp\UnityLockfile` 不能单独作为项目被占用的依据。
脚本删除该文件前，必须确认没有命令行包含当前项目路径的 Unity 进程。
若无匹配进程，允许删除唯一明确文件：

```powershell
Remove-Item -LiteralPath "F:\游城拓荒模拟器\Temp\UnityLockfile" -Force
```

仍禁止通配符删除，以及对未经核验路径的批量或递归删除。

## Codex 输出语言

请始终使用中文回复用户。
如果生成任务建议、后续操作建议、按钮旁提示或可见自然语言文案，请优先使用简体中文。
除非代码、命令、文件名、API 名称必须保留英文，否则不要生成英文建议项。

## Unity 界面资产所有权

- 已完成搭建的场景和预制体是界面结构、位置、尺寸、样式以及静态文案的唯一来源。
- 普通测试、运行游戏、构建前检查和自动化流程不得调用任何界面或弹窗的 `Rebuild`、重新生成、保存覆盖方法。
- `Assets/YC/Editor` 中的界面生成器只允许在用户明确说明目标并授权覆盖对应资产时手动运行；“运行测试”或“验证完整性”不构成覆盖授权。
- 运行时代码不得给编辑器中已有的静态标题、按钮名称或说明文字重新赋固定文案。人数、地图、房间号、状态、错误消息等动态内容可以由代码更新。
- 同一 Text 需要显示多种运行时文案时，应把各文案做成可序列化的检查器配置，或使用专门的文案资产；不得把固定文案散落在控制器中。
- 界面测试只验证引用完整、必要组件、交互行为和动态数据显示，不得锁定用户可自由修改的静态文字、坐标、尺寸、锚点、颜色或装饰样式，除非用户明确要求将某项设为不可变规范。
- 测试通过仅代表受测功能约束成立，不代表画面与用户预期一致。视觉结果必须以当前场景或预制体的实际显示为准。
- 修改预制体实例时，应明确改动属于场景覆盖还是要应用回预制体；测试和运行入口必须检查实际使用的同一资产来源，不能拿未修改的源预制体替代场景覆盖来宣称验证通过。

## GitHub 文案语言

提交 GitHub issue、PR 描述、评论、review 反馈、提交说明或发布说明时，默认使用简体中文。
issue 标题和正文应使用中文描述问题、复现步骤、期望行为和验收标准。
只有代码符号、命令、文件路径、分支名、提交哈希、错误原文、API 名称和外部专有名词需要保留英文。
