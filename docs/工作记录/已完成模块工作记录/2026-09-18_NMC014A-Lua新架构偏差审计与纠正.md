仍不符合

# NMC-014A Lua 新架构偏差审计与纠正

执行日期：2026-09-18  
执行依据：`G:\NoMadCity\prompt\NMC-014A-Lua新架构偏差审计与纠正.md`

## 结论

本轮继续修复后，NMC014A 相关的主链收尾、城市样式清理、资源收集屏障和本地四人自动跑局已闭环；但仍不能宣称 Lua 新架构最终迁移完成。剩余阻塞集中在通用 Lua Effect 的真实领域语义、角色牌/设施/城市样式的 C# 兼容分支，以及完整 EditMode 尚未归零。

## 契约与迁移矩阵

| 范围 | 当前结果 | 证据/剩余风险 |
| --- | --- | --- |
| Lua Host 公共构造器 | 已修正 | `InstallEffectConstructors` 只保留文档 API；已移除 `Effect.GainResourceToOpponents`、`Effect.CharacterAbility`、`Effect.CityStyleSpecialAction`、`Effect.GrantMainActions`、`Effect.FacilityEntry` 的 Lua 暴露。 |
| 文档 API 入口 | 部分完成 | `Condition`、`MoveCity`、`RevealEventCard`、`ResolveCharacterCardEffect`、`OpenPlayerTaskGroup` 已归一化并进入编译器/注册表；首批其他 API 仍由 `GenericLuaEffectExecutor` 以完成占位返回。 |
| 资源玩家引用 | 已修正 | 支持当前玩家引用和 `GameData.GetPlayers()` 返回的稳定 `PlayerRef`；裸字符串指向未知/非当前玩家时仍被拒绝。NMC012 通过。 |
| 影响力与候选集 | 已修正 | `PlaceInfluence.targetSlotId` 可选；`RemoveInfluence.candidateScope` 可用；`Candidate.Add/Remove/Intersect` 与稳定顺序补丁链已接入。 |
| 角色牌十项内容 | 部分完成 | 十项激活事件均有版本化 Lua handler；雷蛇策略已改为两个无预设目标的独立 `PlaceInfluence` 子节点。其余角色目前仍由 `ResolveCharacterCardEffect` 进入 C# `CharacterAbilityCatalog` 执行器，尚未完成 Lua 语义迁移。 |
| 设施内容 | 部分完成 | 资源奖励和护航调度中心已返回通用 Lua Effect；其他设施入口仍返回 `ExecuteMainAction`，对应通用执行器当前为占位完成，且 `FacilityEntryEffectExecutor` 兼容实现仍在。 |
| 城市样式内容 | 部分完成 | 军工区、动员支援和高效机动已返回通用 Effect；复合动力、源石工业枢纽仍有 C# 兼容执行器。`PlayerCleanupStarted` 的 Lua `operation` 字段现已与旧清理执行器归一，清理标记实跑通过。 |
| 回合主链 | 已修正本轮阻塞 | 玩家收尾 Event 只挂一次 Lua 清理子树；空收尾自动推进；资源收集按全员屏障推进；最终回合可直接进入 `FinalScoring` 并由共享主链服务解析。仍有旧兼容路径和部分旧测试假设。 |
| 故障与事件 | 已修正 | `Fault` 向祖先传播至根并进入 `paused_fault`；普通 `Failed` 不再等同于系统故障；完成事件在终态前提交。候选集与事件响应继续经过统一边界。 |

## 本次净变更

- 新增并注册 `GenericLuaEffectExecutor` 与文档 API 稳定 ID，收敛 Host、编译器和注册表入口。
- 收紧 MoonSharp Host 的字段白名单、稳定引用读取、影响力目标可选规则和候选补丁响应；删除旧 Lua 专用构造器的运行时注册。
- 新增 `PlayerRef` 资源目标归一化，事件牌对手奖励改为遍历 `GameData.GetPlayers()` 后逐个生成 `Effect.GainResource`。
- 将角色目录、设施目录、城市样式目录切换为版本化 Lua handler；雷蛇策略和护航调度中心使用两个独立、无目标预设的放置子节点。
- 将建设、探索、移动、部署、调度、角色牌、特殊行动等命令接入当前玩家行动窗口的 Effect 挂载尝试。
- 修正清理窗口、资源收集版本重基线、角色牌 Lua 子节点完成后的统一卡牌生命周期收尾，以及城市移动完成事件的 Lua 观察者链。
- 修正 `PlayerCleanupStarted` Lua handler 使用 `operation` 而旧 C# 清理执行器只读取 `mode` 的契约偏差；补充 Lua Event 到城市样式标记清理的端到端回归。
- 让本地自动跑局复用同一个 `RoundExecutionService` / `FinalScoringService`；回答入口跳过已由采集命令处理的 `collection.task` 二次推进，并把已接受的最后一名玩家采集命令计入统计。
- 更新 NMC005、NMC010、NMC012、NMC013、NMC014 的架构合同测试，并新增本工作记录。未执行批量删除；保留工作区既有改动。

## 验证记录

Unity：`D:\2022.3.62f1c1\Editor\Unity.exe`  
项目：`G:\NoMadCity`

| 测试 | 结果 | XML |
| --- | --- | --- |
| NMC005 资源收集/清理回归 | 1/1 通过 | `Temp\NMC014A-debug-collection.xml` |
| NMC010 角色牌/盖放回归 | 6/6 通过 | `Temp\NMC014A-debug-NMC010.xml` |
| NMC012 事件牌/移动回归 | 11/11 通过 | `Temp\NMC014A-debug-NMC012-rerun.xml` |
| Lua Host + NMC012 | 23/23 通过 | `Temp\NMC014A-host-final.xml` |
| NMC004–NMC014 综合筛选 | 90/90 通过 | `Temp\NMC014A-filtered-final-rerun.xml` |
| 全 EditMode | 1045/1081 通过，36 失败 | `Temp\NMC014A-editmode-full-final2.xml` |
| NMC013 城市样式 + NMC014 设施迁移 | 13/13 通过 | `Logs\EditModeTests\NMC014A-NMC013-final.xml` |
| NMC005 回合主链 | 9/9 通过 | `Logs\EditModeTests\NMC014A-NMC005-after-collection-skip.xml` |
| 四人本地联机自动跑局 | 1/1 通过 | `Logs\EditModeTests\NMC014A-localhost-autoplay-final.xml` |

此前全 EditMode 的 36 项失败主要落在旧回合主链/特殊行动兼容假设、既有 Presentation 动态构造数量约束、设施目录 SHA-256 资产约束、旧自动跑局路径和少量旧资源收集/入场事件测试。本轮未重新宣称全量通过；上述专项结果只证明本轮修复的目标路径已通过。

## 仍需处理的阻塞项

1. `GenericLuaEffectExecutor` 当前对已登记的选择、重复、骰子、设施卡、运营商、企业、主行动和部分城市样式类型仍统一返回 `completed`，尚未真正修改领域状态或创建所需交互；这仍是最大的架构阻塞。
2. 角色牌除雷蛇策略外仍通过 `ResolveCharacterCardEffect` + `CharacterAbilityCatalog` 的 C# 执行器承载卡面语义，十项内容尚未全部改为 Lua 通用 Effect 组合。
3. 设施入口和城市样式旧 C# 执行器、兼容工厂及部分旧字段仍存在；需要在通用 Effect 有真实语义后逐项移除，而不是继续保留适配器。当前 `operation`/`mode` 兼容只是迁移过渡，不是最终架构答案。
4. 全 EditMode 的历史基线仍有 36 项失败；需要在专项迁移完成后重新跑全量，区分旧测试合同、资产约束和真实回归，最终降为 0 失败。
5. 本轮已验证四人自动跑局，但仍需补齐 2/3/4 人空收尾、部分玩家收尾、多个收尾排序、恢复幂等和完整 Event 幂等矩阵，不能用单个自动跑局替代多人权威测试。

## 下一步验收标准

- 为所有已登记文档 API 提供真实领域执行器，禁止占位完成。
- 角色、设施、城市样式内容只由 Lua handler 组合文档 API，删除 C# 卡面/行为族分支及旧入口工厂。
- 全 EditMode 回归恢复为 0 失败，并补齐 2/3/4 人、故障暂停、候选顺序、完成事件幂等和最终计分生命周期证据。
