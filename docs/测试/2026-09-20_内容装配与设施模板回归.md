# 2026-09-20 内容装配与设施模板回归

## 变更

- `enabledExpansionIds` 接入内容选择，显式 replaces 接入实际替换。稳定运行时槽位沿用根定义 ID，来源定义保留其自身 ID/扩展；只注册最终角色能力并读取最终数据/名称/贴图。
- 拒绝启用替换的缺失/未启用目标、冲突替换与循环，不用清单加载顺序决定胜者。启用配置纳入内容包哈希，恢复不静默换包。
- 设施改为 19 份共用模板与 46 份实体差异引用，JSON 展开数据已与迁移前快照逐项比较一致；现有完整目录初始化回归保持通过。
- 外部 Texture2D 缓存改为覆盖失效条目，避免销毁后重载的重复键异常；补充销毁后真实解码测试。

## 自动化结果

1. `Logs/content-composition.xml`：1179/1179 通过，0 失败/跳过，33.6134924 秒，Unity/测试脚本退出 0。
2. 此后仅新增设施、事件、城市样式替换测试，生产代码未变；`Logs/content-composition-typed.xml` 中 ExternalContentPackTests 38/38 通过，1.6459899 秒，退出 0。
3. 回归过滤 `!YC.Tests.EditMode.LocalhostAutoplayRunnerTests;!YC.Tests.EditMode.MultiplayerLaunchRegressionTests`，排除 5 项慢用例。两轮不是一次“1182 全量运行”，不宣称完整联机通过。
4. 新增覆盖真实角色命令改用替换脚本、初始卡位不变、图片别名、扩展停用还原、存档哈希不匹配拒绝、逆序替换链、扩展依赖与冲突、模板字段合并/数组覆盖/文件快照和 3 类模板错误，以及三种非角色定义投影。
5. 原 46 张完整设施 data 比较快照位于 `Logs/facility-template-migration-before.json`。该快照仅用于本轮迁移核对，不是正式内容来源。

## 边界

- 用户指定永续/一次性生命周期等到具体扩展卡牌实现，本轮仅保留其定义字段与校验。
- 企业与企业家玩法仍未启用；目录原有数量/颜色/行为族限制仍存在。设施多种行为仍经过旧 facility_entry 适配，模板拆分不是这些规则全部纯 Lua 化。
- 扩展文件须进入同一包的 definitions 清单；未做独立 ZIP/目录自动扫描、下载或菜单选择 UI。
- 未运行独立 Player 构建或 Steam/多机联机；NMC-015 不结案。


## 正式启动玩家流程

最终代码运行 `Run-PlayerJourneyEditor.ps1`，PJ-001/liskarm-strategy，seed 2026091901。八回合、102 步、0 错误；16 次部署确认、1 次取消、1 次角色能力、21 次地图选择、1 次收尾移除；终局计分可见并返回开始菜单。已检查游戏内截图，卡牌/城市样式与地图正常显示。

证据：`Logs/PlayerJourney/20260920-content-composition/result.json`、`actions.jsonl`、`unity.log`、`success.png`。本次为核心扩展配置的 Editor 玩家流程，不代表所有替换扩展或所有设施特殊效果均经过完整 UI 跑局；替换逻辑另由上述自动化命令/投影测试验证。
