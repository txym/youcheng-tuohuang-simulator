# 2026-09-20 外部定义元数据回归

## 实施范围

- 外部包 schemaVersion 2，79 项核心定义与 3 个未启用企业/企业家模板补齐 expansionId/replaces。
- 每项角色能力新增 skillType（normal/persistent/one_shot）；永续技能明确 hasSpecialZone，启用时校验 specialZoneId 引用同卡 playerMarkerZones。
- 替换引用包含目标扩展、类型、ID；拒绝跨类、自替换、包内扩展不匹配和循环。未加载目标仅保留声明，不自动应用替换。
- 核心角色十项能力当前均为普通。没有改动卡面脚本、UI 布局或牌组；永续/一次性运行时生命周期与特殊区域 UI 不属于本轮已实现范围。
- 设施核实：46 个完整 JSON 对应 46 张实体牌，名称和 effectId 各去重为 19 种，没有转换为模板/实例分离结构。

## 验证

- Unity 2022.3.62f1c1，EditMode，**1168/1168 通过，0 失败、0 跳过**，执行 34.363335 秒，Unity/脚本退出码 0。
- `Logs/content-metadata.xml`、`Logs/content-metadata.log`。
- 同前轮过滤两个 fixture：`!YC.Tests.EditMode.LocalhostAutoplayRunnerTests;!YC.Tests.EditMode.MultiplayerLaunchRegressionTests`，排除 5 项慢用例，不是完整联机回归。
- 本轮新增 15 项：4 种合法类型/区域组合及替换元数据读回、10 种错误定义拒绝、包内替换目标归属和循环校验。
- 核心包实际加载、目录初始化兼容、自定义角色命令执行、原有十项角色能力及 UI 合同均随回归通过。
- 未重跑玩家黑盒/Player 构建；前轮雷蛇八回合记录不能作为新增永续或扩展替换功能的验证。
