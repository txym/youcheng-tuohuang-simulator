# 设施 Lua 与动态目录回归

## 本轮实施范围

- 五类设施迁到外部 Lua：出售资源、替换或放置影响力、任选五基本资源、免费移动后航道放置、核心相邻设施奖励。沿用现有数量弹窗和地图选择，先交互再由内核变更状态。
- 新增 ChooseResources 通用资源分配与严格提交校验；公开已建设设施快照；免费移动完成后才生成实际经过航道的选择。
- 外部设施、事件、城市样式和特殊行动目录解除固定数量与 ID 限制；事件 Lua 注册从实际颜色池遍历，设施与城市样式脚本从有效外部定义注册。
- 设施正式来源改成 19 组颜色数量清单加共用模板，展开 46 张实体；保留全部历史 ID、各自图片与供应分类。旧 46 份实体 JSON 保留参考但不再被正式清单引用。
- 正式行动处理器共用回合服务与注册表；撤下已迁移的五类旧设施 behavior 分支，宿主版本升至 1.1.0，旧节点不能静默继续。

## 自动测试

最终命令使用 `tools/tests/Run-EditModeTests.ps1`，过滤器为 `!YC.Tests.EditMode.LocalhostAutoplayRunnerTests;!YC.Tests.EditMode.MultiplayerLaunchRegressionTests`。

- **1203/1203 通过**，失败 0、跳过 0，测试执行 37.286 秒。
- 结果：`Logs/migration-complete-regression.xml`；日志：同名 `.log`。
- 排除的两个慢测试 fixture 共 5 项，本轮未运行，不能称全量联机回归。
- 新增设施组合测试 12 项：等待时恢复、重复提交、全零出售、非法分配不改状态、分支执行、己方相邻计数、移动后航道选择和退休行为节点拒绝。
- 新增动态目录测试 9 项：新设施 ID/effectId 实际 Lua 激活及恢复、数量增加保持 ID、五类坏清单拒绝、新事件真实注册与抽牌、新城市样式/行动 ID 及费用。
- 专项前次 20/20 通过；最终新增退休行为节点用例后，21 项均纳入上述 1203 回归。

测试发现并修正：设施移动 continuation 错用主 handler ID；资源分配答案被通用候选 ID 校验错误拒绝；事件 Lua 注册仍遍历固定 22 张；新测试修改共享目录快照导致后续测试污染。最终结果对应修复后的代码。

额外静态校验：核心 JSON 全部可解析，pack 与 facility_inventory 的 JSON Schema 校验通过；19 组生成 46 个唯一 ID，与原实体文件 ID 集合一致。蓝、黄、红各 11 张，彩色 13 张。

## 实际玩家流程

运行 `Run-PlayerJourneyEditor.ps1 -Scenario PJ-001 -CharacterScenario liskarm-strategy`，seed 为 2026091901。结果目录：`Logs/PlayerJourney/20260920-facility-dynamic/`。

- success=true；八回合，102 步，0 错误。
- 16 次部署确认、1 次部署取消、1 次角色激活、1 次收尾移除。
- 终局计分可见，并成功返回开始菜单；已检查 success.png。
- result.json、actions.jsonl、unity.log 和截图保留供复查。

这是实际 UI 启动与回合链的回归，不覆盖五类设施的全部 UI 分支；设施专项目前由内核集成测试覆盖，对应人工检查项已加入玩家黑盒方案。本轮未构建独立 Player，也未重新验收 Steam 联机。

## 剩余边界

复制、额外建设、扩展枢纽等设施仍有行为适配，旧 Pending 兼容调用尚未全部移除。NMC-015 不能结案；完整候选补丁链及发布级联机验收等仍需完成。永续、一次性技能生命周期和企业、企业家具体玩法按既定约定留待扩展卡实现。

架构与内容格式已同步到《外部内容包与Lua定义》《Lua效果系统API初稿》《Lua新架构迁移结案》和核心包 README。没有改动现有 UI 布局。
