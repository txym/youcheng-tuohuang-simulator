# 相邻设施入场复制回归

附属能源设施改为外部 Lua：查询己方十字相邻的非彩色入场设施，Choice 选择后调用通用 ActivateFacilityEntry。沿用设施选择弹窗，旧 ReplayAdjacentEntry 结算分支撤下，设施入口宿主升至 1.2.0。

新增两项集成测试，覆盖合法邻居筛选（排除远处与对手设施）、选择前不发奖励、等待时恢复、重复提交不重复发奖，以及没有合法邻居时正常完成。设施实例激活原语重新检查实际实例归属和入场资格，并等待子效果完成。

最终 EditMode **1205/1205 通过**，失败 0、跳过 0，耗时 35.9469486 秒。结果文件 `Logs/facility-copy-final.xml`，日志 `Logs/facility-copy-final.log`。过滤器为 `!YC.Tests.EditMode.LocalhostAutoplayRunnerTests;!YC.Tests.EditMode.MultiplayerLaunchRegressionTests`，两个慢 fixture 共 5 项未运行。

首次编译发现快照局部变量重名，已修正；首次回归新增两个用例发现 Lua 沙箱未开放 math，改用基础整数运算判断相邻后复跑通过。未扩大沙箱权限。

本轮未运行真实玩家 UI 黑盒、独立 Player 构建或联机验收。旧选择弹窗仍使用原候选格式；名称展示与点击链需要按玩家黑盒文档检查，不将内核测试等同 UI 验收。

审查发现但尚未修复：载具仓库旧适配缺少“移除后调度”分支，探索候选交互也不完整。需补齐通用探索选择并迁移完整分支。额外建设、扩展枢纽、收尾标记及旧 Pending 路径仍未全部迁移，NMC-015 不可结案。

架构、API、外部包 README 和玩家黑盒方案已同步更新。永续及一次性生命周期仍按用户约定留待具体扩展卡实现。
