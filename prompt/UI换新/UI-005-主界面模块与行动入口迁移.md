# UI-005：主界面模块与行动入口迁移

请执行本任务。先读 [共同约束](00-共同执行约束.md)、[实施计划](01-UI更新实施计划.md)、[视觉搭建优先规范](03-视觉搭建优先与分阶段验收.md)、[05 任务安排](05-地图俯视改造期间的任务安排.md)及 UI-001～UI-003 记录。前置：新主界面的静态视觉结构已在 UI-003 完成。UI-004 暂缓，本任务可先推进；世界地图相关端到端验收等俯视改造交付后补齐。

2026-09-25 补充：同时执行[主界面按钮 3.2 补充提示词](UI-005-补充-主界面按钮3.2.md)，按 [04 增量规范](04-主界面按钮3.2增量规范.md)先完成新到的 11 个按钮视觉和层级，再接本任务业务。原任务内容保留，旧角色牌／宣告样式入口与建设／特殊行动不能按位置互换。

## 目标与范围

为 UI-003 已搭好的玩家状态、城市、企业家、手牌、合作区、牌堆和行动页签连接真实游戏数据及行动流程。主要目标为共享 HUD、`ActionPanel`、`CharacterHandPanel`、资源／回合相关 View 及两场景绑定。本任务不再承担“首次搭建主模块边框与底图”；发现前置视觉缺项时先补齐再继续业务接线。

## 工作步骤

1. 复核 UI-003 逐区视觉清单及素材依据，保留已经核对的主界面构图与模块结构，复用集中适配配置；禁止逐个照抄 1920×1080 坐标到控制器。两栏和 `ContentRect` 使用 UI-003 的来源，新增动态条目先确认贴图／字体／层级完整再接行为。
2. 玩家数量、顺序、回合、分数、资源和标记从当前可见投影读取；支持长名字和多位数。收起侧栏和紧凑布局保留同一份选择、页签及滚动状态。
3. 手牌、企业家、弃牌和盖放角色按实际集合显示。图包的可见槽数只是布局容量，超量内容提供滚动／展开；隐藏卡不能通过缩略图、详情或工具提示泄露。
4. 合作轨道与城市样式标记保留各玩家独立状态。多人同级组合色块使用素材映射，缺少规则状态时不要用演示等级填充。
5. 右侧按主要行动、快速行动、城市样式组织入口，结束行动统一到常驻底栏。功能迁移到新入口后停用对应旧可操作入口，避免两个按钮同时提交或响应快捷键。
6. 对每项行动跟踪启用条件、候选来源、取消、返回、确认和失败提示。现有主要行动预选与新 `InteractionRequestRouter` 之间明确输入所有权；不在本项顺带重写 Domain 主链。
7. 使用当前动作能力驱动禁用状态。非行动玩家、必选交互、pending、断线等状态仍可查看合法信息和使用两栏合法功能。
8. 紧凑模式让次要模块通过稳定入口打开，不能将行动按钮裁在屏幕外；长列表和卡面详情仍能操作。
9. 主界面详情与资料入口接入 UI-003 的唯一页面管理。effect 页收起后可以查看其他资料，切换资料关闭前页，顶栏展开时让出页面并恢复 effect；查看行为不能触发隐藏 effect 的地图选点或提交。侧栏自适应收放使用独立入口，不复用顶栏 effect 按钮。
10. 使用 UI-002 的字体角色配置；常规／强调文字分别为方正／汉仪，汉仪不勾选 Bold，特殊词使用 msyh。资源 icon 前数量等效果数字用 NarrowBold；顶栏回合数等纯 UI 数字用 wide，混排按语义分段。
11. 本项保留当前地图相机、姿态、投影和取景控制，承接已有中央地图区域接口。UI 按钮／遮罩命中仍做必要检查；若迁移城市板或设施供应交互，核对被改动的城市映射。世界地图点击、导航边界和依赖地图选点的完整链交给俯视改造后的 UI-004。迁移未验证前保留原可用入口，勿为继续 UI 工作顺带重做相机。

## 验收

- 两实际场景可以通过新版入口完成已有主要行动及快速行动代表流程；每次提交只产生一次正式意图。
- 当前页签、选择、手牌状态在折叠／展开、弹窗返回和尺寸变化后合理恢复。
- 正确显示自身／对手信息，未知或隐藏内容不泄露；空牌堆、超量手牌与资源增位可用。
- 顶底栏、地图及新模块不争用输入，旧重复入口已停用且相关引用没有丢失。
- effect 收起后的资料浏览与展开恢复流程可用，同时最多一页，不绕过业务限制；实际文字与数字符合五类字体规范。
- 实际画面覆盖基准、4:3、超宽和紧凑状态；保留用户可编辑的静态文案与样式。

上述验收先完成不依赖世界地图改造的部分。需要最终地图选点的行动链在记录中明确标为待验，保留入口和上下文检查结果；不能把按钮可点击当作完整行动已通过，也不因整套地图矩阵尚待执行而阻挡独立 UI 工作。

新增 `执行记录/YYYY-MM-DD_UI-005_主界面接入.md`，列出模块与数据源、新旧入口切换表、场景证据和留给窗口任务的明确缺口。
