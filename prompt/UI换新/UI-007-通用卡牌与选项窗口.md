# UI-007：通用卡牌与选项窗口

请执行本任务。先读 [共同约束](00-共同执行约束.md)、[实施计划](01-UI更新实施计划.md) 和 UI-006 的接入约定。前置：共用组件、内容层级、结算上下文均可用。

## 目标与范围

把通用卡牌单选／多选、普通选项单选和重复出售接入新样式及自适应窗口。修改实际 `EffectDialogShell`、条目模板、布局配置、相关 renderer 和 Registry 引用；按 UI-001 核实的正式入口迁移。

## 阅读入口

- `游城拓荒/UI素材/UI_LAYOUT.md`、`OPTION_WINDOWS.md` 及对应整理参数。
- `EffectDialogShellView`、`EffectDialogOptionRowView`、`EffectDialogResourceRowView`、`EffectDialogLayoutProfile`。
- `CardVisualCatalog`、`CardInteractionLayoutProfile`、`InteractionRequestRouter` 和当前相关请求类型。

## 工作步骤

按[视觉搭建优先规范](03-视觉搭建优先与分阶段验收.md)先完整搭建窗口外框、底图、标题、卡槽／选项条、按钮、滚动区及文字图标，再核对层级和适配，随后接请求逻辑。验收截图须等待当前状态必需素材和布局就绪。

1. 复用新框体、按钮和卡槽；原选中框按其透明留白与实际卡面映射。窗口、遮罩、详情查看器都在内容区，不盖住顶底栏。窗口不支持用户拖动移位；接续 UI-003 检查 `EffectDialogDragHandle` 的使用、`EffectDialogShellView` 的必需引用校验和运行时启用路径，防止打开窗口或切换模式时恢复拖窗。滚动和卡牌内容交互独立保留。
2. 以可用宽高及卡牌最小可读尺寸选择列数／滚动方式，支持 0、1、少量、7、超过 7 和大量候选。旧素材的数量分档是参考，不能强行保留导致小屏溢出。
3. 单选与多选只修改当前请求的草稿，明确已选数量、最少／最多数量、确认条件和取消权限。查看详情不等于选中；返回详情后保留草稿。
4. 普通选项使用稳定 ID，连续阶段切换时清理不属于新请求的旧选项。失效或不可选内容可以按规则显示原因，不能提交过期目标。
5. 重复出售使用每种资源的整数数量草稿，约束在当前库存范围。全零不能确认出售，结束出售按规则允许零成交退出；整批只提交一次。
6. 分开显示库存、预期收入及已成交结果；实际价格和收入由规则提供。库存变化／被拒绝时刷新并复核草稿，成功后按真实结果清理。
7. 请求仍由统一 router 分发，详情与子选择共享 UI-006 上下文，并通过唯一页面切换或当前页内部步骤展示，不同时显示父子页。收起保留当前有效草稿并释放输入，展开先关闭资料页再复核并恢复当前步骤；同一请求刷新不强制重开。区分 renderer 的业务清理和页面隐藏，保证 pending、防重入、返回和过期响应处理一致。
8. 静态标题、说明、按钮文案按窗口模式配置到资产。不要把图包演示角色步骤、浏览器事件或示例库存作为正式规则。
9. 使用 UI-002 的五类字体配置；选择次数、资源 icon 前数量、效果费用和收入数字使用 NarrowBold，纯 UI 页码使用 wide。“策略”“计谋”等特殊词用 msyh，普通／强调文字分别用方正／汉仪，汉仪不勾选 Bold。数字与中文按语义混排。

## 验收

- 实际单选、多选和选项业务入口可用；空状态、超量候选、滚动末项和数量边界正确。
- 出售数量增减、库存变动、零成交结束、整批成功／拒绝正确，预选不提前结算。
- 详情返回、切换请求和调整窗口大小后状态一致；隐藏候选／卡面遵守玩家权限。
- 主页面、独立抽屉、详情及子选择互斥切换，两栏仍可操作，UI 点击不穿透到地图；effect 收起后可查看资料，展开关闭资料并恢复有效草稿，全程最多一页。
- 拖动窗口标题、背景和边缘不移动窗口；关闭重开、切换模式／请求后仍不可拖窗，内部列表与卡牌操作正常。

新增 `执行记录/YYYY-MM-DD_UI-007_通用选择窗口.md`，记录正式请求与 renderer 映射、窗口资产、验证尺寸和各模式结果。
