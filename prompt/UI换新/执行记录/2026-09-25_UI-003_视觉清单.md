# UI-003 主界面视觉搭建清单

日期：2026-09-25。旧的 `UI003-*.png` 截图是搭建前诊断；`UI003-Diagnostic-*` 是逐步修正时的诊断图。最终验收以资源、布局和视口就绪后重新生成的截图为准。

| 区域 | 依据 | 新版素材 ID | 当前正式节点／来源 | 当前状态与缺项 |
| --- | --- | --- | --- | --- |
| 主外框 | `layout-patch` 1920×1080 全屏、`main-idle` | `outer-frame` | 共享 HUD 的 `Mobile City UI Canvas/UI003 Main Surface/Outer Frame` | 四边及角部可见、中心透明，装饰不接射线；绘制顺序位于模块边框下方。 |
| 顶栏 | `topbar-detail`、`layout-patch.topbar` | `topbar-bg/frame`、`icon-frontier`、`round-plaque`、`turn-tag-refined`、`redzone-plaque`、`icon-action/redzone/panels/settings` | `Persistent Gameplay Bars/Screen Safe Area/Persistent Top Bar` | 品牌、回合块／刻度、阶段、行动旗标、红区分组、收起与设置入口已搭；数字和状态使用现有数据。 |
| 对手信息 | `layout-patch.opponents`、`main-idle` | `module-bg/frame`、`paper-bg`、`header-bg`、`card-bg/frame` | `UI003 Main Surface/Main Regions/Opponent Seat 1～3` | 三个席位框与头像槽完整；当前单人诊断显示“等待玩家”，真实对手资料接入留给 UI-005。 |
| 我的城市 | `layout-patch.city`、`main-idle` | `module-bg/frame`、`header-bg`、实际 `city_board` | `Main Regions/City Region/City Board Artwork` | 使用当前项目原城市板贴图完整等比显示；原 `BuildInfoPanel` 城市格位仍在旧 View 内，精确交互需 UI-004／005 迁移。 |
| 地图 | `layout-patch.map`、`main-idle` | `module-frame`、实际 MapView | `Main Regions/Map Region`，实际 `MapDisplayController` 相机视口 | 真实地图渲染在中央视口；两张地图预制体的初始视口缩放已恢复为 1.2。 |
| 自身状态／企业家／手牌 | `layout-patch.selfSummary/entrepreneurs/hand`、`main-idle` | `module-bg/frame`、`paper-bg`、`header-bg`、`card-bg/frame`、项目资源图标 | `Main Regions/Self Summary Region`、`Entrepreneurs Region`、`Hand Region` | 自身姓名／资源数接实际状态；五个企业家槽及手牌容器在位，手牌实际内容由原 `CharacterHandPanel` 承载。 |
| 合作／弃牌／盖放 | `layout-patch.cooperation/discard/faceDown`、`main-idle` | `module-bg/frame`、`paper-bg`、`header-bg`、`card-bg/frame` | `Main Regions/Cooperation Region`、`Discard Region`、`Face Down Region` | 合作轨道、空态与卡位完整；弃牌沿用实际按钮，真实合作及盖放数据留给 UI-005。 |
| 页签与行动 | `layout-patch.rightTabs/rightPage`、`main-idle` | `module-bg/frame`、`header-bg`、`button-primary/secondary/disabled` | `Main Regions/Action Tabs`、`Action Region/Action Content/Action Panel` | 三页签及主要行动区完整；原行动按钮已移动并换新样式。页签业务切换和设施供应展示仍需 UI-005。 |
| 底栏 | `layout-patch.bottomBar/undo/resolutionSummary/endAction`、`main-idle` | `bottom-bar/frame`、`chain-slot-bg/frame`、`undo-disabled`、`icon-undo-muted`、`button-primary/disabled` | `Persistent Gameplay Bars/.../Persistent Bottom Bar` | 摘要槽、撤销禁用态、唯一结束行动按钮完整；撤销能力未提供。 |

所有新版 PNG 已由 UI-002 导入 `Assets/YC/Presentation/Ui002/Sprites/Frontier31/`；逐文件 GUID、切片和来源哈希见 `2026-09-24_UI-002_资产映射.json`。关键 GUID：`outer-frame` 为 `aa1b97a5365d1bc43a2347ecf9652512`，`module-bg` 为 `3b717c670b1a52843bcc7592be6fe504`，`module-frame` 为 `b4ca9e75c1cae354aa1b2ce17d12d467`，`paper-bg` 为 `9abda3d4730078e43951140a94b0d95f`，`header-bg` 为 `8cc4ba9975aca514aa6e0a19d59ee8ff`。两实际场景引用同一个共享 HUD 源 Prefab，未保存场景覆盖；地图的两个源 Prefab 分别保存了初始视口缩放配置。静态排版位于资产，不在运行时生成。

仍需明确移交：主画面目前显示同源城市板贴图，旧 `BuildInfoPanel` 的可交互城市格位和设施供应留在原 View；存在可拖动设施候选或建设草稿活跃时临时打开该旧 View 并置于主视觉之上，以保留现有建设入口。UI-004／005 应将真实格位与设施供应迁入新版区域，再删除临时适配。1024×768 等宽度受限窗口通过“左侧信息／地图／行动”分区导航横向切换，提高内容尺寸并保持各区可达；900×600 受高度约束，保持三列整体等比显示，文字较小，后续可继续细化低高度排版。
