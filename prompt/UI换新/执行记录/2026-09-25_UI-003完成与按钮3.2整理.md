# UI-003 完成交接与按钮 3.2 素材整理

日期：2026-09-25。工作类型：只读核对现有交付、新增素材目录、补充实施计划与提示词。本轮没有运行 Unity、生成器或源包补丁安装脚本，没有修改项目代码、场景、Prefab、字体或已导入纹理。

## 1. 来源与 UI-003 状态

用户确认 UI-003 完成，并新增 `MainButtons-v3.2-Patch.zip` 与 `MainButtons-v3.2-Notes.md`。

读取 [UI-003 交付记录](2026-09-25_UI-003_常驻栏与适配框架.md)及[视觉清单](2026-09-25_UI-003_视觉清单.md)，查看 [SampleScene 实际基准图](证据/UI003-LayerFix-SampleScene-1920x1080-actual-1920x1080.png)。索引据此改为“框架范围已完成”，不代表 UI-004 之后的业务与综合验收完成。

两场景框架画面、层级修正和 4／4 PlayMode 结果沿用原交付记录；本轮未重新运行这些验证。旧建设 View、设施供应／格位迁移、地图精确命中、真实主模块数据及完整行动业务继续按原交接推进。

## 2. 新增目录与保留范围

- [按钮增量目录](../../../游城拓荒/UI素材/整理增量-2026-09-25-按钮3.2/README.md)：与旧整理版并存。
- `原包/` 按包内相对路径逐字节复制 174 个文件，保留原说明、JSON、预览及脚本内容；脚本只归档不执行。
- `清单/` 新增源文件快照、源包文件索引、逐按钮逐状态贴图索引和本轮校验报告。
- 新增[整理及只读校验脚本](../../../tools/prepare_ui_button_patch_catalog.py)，目标已存在时只允许校验，不覆盖已有目录。
- 原 ZIP、原说明、2026-09-24 整理版、Lua 分类中的原提示词、UI-001～003 的交付记录与截图均保留。更新当前分类索引和计划，原分步提示词通过补充链接接入新要求。

## 3. 素材核对

| 范围 | 结论 |
| --- | --- |
| 版本 | 基于 Frontier 内部 3.1，仅覆盖指定按钮的 3.2 增量 |
| 替换对象 | 三个页签、六个主要行动、撤销、结束行动，共 11 个 |
| 素材 | 4 个底板系列、18 个底板状态、33 个图标配色；51 项逻辑素材各有 1x PNG／2x PNG／SVG，共 153 份源素材 |
| 九宫格 | 1x 切片四边 12，2x 四边 24；不得沿用 3.1 切片 |
| 状态 | 禁用优先；页签选中独立保持，含 selected-hover；图标和文字独立于底板 |
| 范围外 | 设施供应区按钮、顶栏、城市及合作模块、底栏结算槽、其他窗口 |

查看了源 `button-sheet.png`、`main-idle.png`，并对照实际 UI-003 基准图。当前旧行动区包含“使用角色牌／宣告样式”，新六格包含“建设／特殊行动”；`ActionPanelView` 的现有引用也证实不能按旧序号换图。`UiSharedButtonState.Refresh()` 会重赋旧主题 Sprite 并叠加 tint，接入时需解决状态驱动冲突。

## 4. 执行安排与文档

顺序继续 **UI-004 → UI-005（含按钮补丁）→ UI-006 及后续**。按钮补丁放在 UI-005 的视觉阶段，与原本已规划的页签／行动入口迁移共同完成，避免先换错事件、之后再次拆迁。

- 新增[04 按钮规范](../04-主界面按钮3.2增量规范.md)及 [UI-005 补充提示词](../UI-005-补充-主界面按钮3.2.md)。
- 更新分类索引、计划和共同约束中的状态／素材优先级；UI-005／006／012 增加补充引用。
- 保留 03 原执行纠正内容，补记 UI-003 已完成，避免旧继续要求被当作当前任务状态。
- 提示词仍按“素材映射 → 完整视觉 → 层级适配 → 业务 → 就绪后实际验收”。本轮没有创建或派发新的实施任务。

## 5. 本轮实际校验

```powershell
python tools/prepare_ui_button_patch_catalog.py --verify
python tools/prepare_ui_asset_catalog.py --verify
```

- 新增目录：174 份原包副本与 4 份清单校验通过。
- 源包清单 173 项哈希／大小通过；该清单自身是额外第 174 个文件。
- 补丁安装清单 161 项源文件哈希／大小通过；没有执行安装。
- Frontier 原包 4 个指定基线哈希均匹配。
- 105 张 PNG 的签名、块 CRC 和压缩数据解压通过，运行时素材尺寸与配置匹配。
- 外部说明与包内 `BUTTON_PATCH.md` 逐字节一致。
- 旧整理版保全校验通过：173 个原文件内容不变（含已分类迁移的 18 个），251 个整理副本与来源一致。
- 检查本轮涉及的 12 份当前文档，117 个本地链接均存在。

未执行浏览器交互、Unity 导入、运行时状态验证和新按钮实际截图；这些由 UI-005 实施后记录。源 `QA.json` 的自报结论仅作为原材料保留。
